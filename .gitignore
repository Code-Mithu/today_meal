node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo
.DS_Store
.vercel

# Environment files (keep the committed template)
.env
.env*
!.env.example

# Android signing keys. Committing these lets anyone publish an update that
# overwrites your Play Store app; a leaked upload key cannot be revoked easily.
*.keystore
*.jks
credentials.json

# Native directories generated by `expo prebuild` (this is a managed workflow)
/android
/ios

# Local EAS build output
*.apk
*.aab

# Python
__pycache__/
*.pyc
*.pyo
server/db.sqlite3
server/staticfiles/
.venv/
venv/
